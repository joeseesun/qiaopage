# 参与开发

这是从既有服务拆分的独立开发仓库。欢迎提交可复现的问题、安装反馈和小范围 PR；云平台改造请先说明存储、事务和数据迁移设计。

1. 使用 Node.js 24+，运行 `npm ci` 和 `npm run setup`。
2. 在功能分支修改，保持 API、权限、稳定 URL 和已有数据兼容。
3. 运行 `npm run check`、`npm test`。修改安装流程运行 `npm run verify:docker`；修改 UI 运行 `npm run verify:ui` 并检查桌面及 390px 截图。
4. PR 描述写清问题、行为变化、验证与限制。测试不能代替真实平台上线验收。

不要提交 `.env`、令牌、数据库、备份、个人路径或生产日志。不要给用户发布内容追加署名、推广或未经选择的 meta。依赖和字体需保留来源许可。

讨论请围绕问题和证据，尊重其他参与者，避免人身攻击或骚扰。维护者可关闭不相关、泄密或不尊重他人的内容。
